---
topic: SaaSify Subscription Tracker
updated: 2026-07-03T15:45
---

- (event) Initialized PRD workspace for SaaSify Subscription Tracker
- (assumption) SaaSify is built as a pure client-side web app using HTML/JS/CSS with localStorage persistence to keep deployment overhead zero.
- (assumption) US Dollar ($) is the default currency used for all spend calculations and UI formatting.
- (assumption) The client system time is the source of truth for the current date to determine renewal alert badges.
- (assumption) A standard browser confirm dialog is used for subscription deletion confirmations.
- (decision) Seed 3-4 mock subscriptions on first load to demonstrate dashboard state and alert badges.
- (decision) Hardcode the 'Renewal Imminent' alert threshold to 7 days for MVP simplicity.
- (decision) Set default role to Viewer, persist role selection in localStorage, restrict renewal alerts to 0-7 days in the future, show modal validation inline, and seed mock data only when localStorage is uninitialized.
- (event) PRD finalized
